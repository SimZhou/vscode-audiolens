# 更新日志

## 1.8.1

修复：多通道 PCM 首次打开的频谱图。

- 修复 1.8.0 的一个回归：打开多通道 PCM 文件（如 2mic1ref 录音）时只有第一个通道显示频谱图，其余通道为空，直到修改设置后才恢复。原因是 worker 采样存储在分析已启动后又被清空，导致延迟渲染的通道读不到采样。采样重置现在改在解码之前执行，不再在解码之后。

## 1.8.0

频谱图缩放与平移实时化。

- 频谱缩放/平移现在实时响应：横向平移复用已计算的 FFT 列，频率轴缩放/平移、调色板和 dB 变化不再重算 FFT，只重新光栅化（通常仅几毫秒）。
- 连续缩放/平移期间最新请求始终优先：前沿节流让分析保持稳定节奏而非越拖越久，被取代的请求在算任何 FFT 前就提前取消。
- 缩放跨越缓存边界时画面保持连贯：用缓存结果按粗→细合成绘制（粗层在下、细层覆盖），配合横向时间裁剪与纵向频率重映射，不再出现重算时的空白间隙；交互停下后预取相邻的平移/缩放范围，让下一次跨边界步进直接命中缓存。
- 底层：Worker 常驻每通道采样并使用实数 FFT + 缓存的 twiddle 表；幅值缓存将 FFT 结果与显示参数解耦，使仅显示相关的改动变得廉价。

## 1.7.0

纵向坐标轴与每通道频率/振幅控制。

- 波形振幅轴与频谱频率轴的刻度数随音轨高度自适应；左侧刻度区更窄，频率标签更紧凑（如 `8k`、`500`）。
- 每条通道的频率轴可独立定制（非持久）：右键设置刻度类型（线性/对数/Mel/Bark/ERB）；`Ctrl`+滚轮 / 捏合缩放、`Shift`+滚轮 / 横向滑动平移该通道频率范围；双击恢复该通道默认。
- 振幅由全局缩放系数改为每通道范围：波形上 `Alt`+滚轮（或轴上 `Ctrl`/捏合）缩放该通道，`Shift`/横向平移，双击重置。设置菜单新增「振幅范围（波形）」全局默认与自适应开关（自适应时每通道各自贴合峰值）。

## 1.6.0

每通道增益与声道平衡播放控件（[#6](https://github.com/SimZhou/vscode-audiolens/issues/6)）。

- 每条通道侧栏（视图选择器下方）新增 Audacity 风格的增益滑块（−24 dB ~ +24 dB）和声道平衡滑块（100% 左 ~ 100% 右），刻度尺样式配圆形滑钮，双击归零。
- 立体声文件可听到真实声像：把 CH 1 拉满左、CH 2 拉满右即为立体声直通；默认仍是原有下混行为（所有启用通道汇总到左右两耳）。
- 增益/声道平衡变化在播放中平滑过渡、无台阶噪声，悬停或拖动时浮动数值提示显示当前值。
- 移除顶部工具栏的全局播放增益滑块，增益控制下放到每条通道。

## 1.5.3

下载对话框默认路径更新。

- 音频下载和选区 WAV 保存对话框预填音频源文件所在目录，依次回退到工作区目录、主目录。Remote SSH 会话不再默认远端根目录。

## 1.5.2

PCM 布局与音轨拖拽细节更新。

- 新增可折叠的 PCM 文件参数面板，读取或保存默认值后保留当前/已保存参数摘要，并为波形图和频谱图释放纵向空间。
- 对齐 PCM 参数面板的标题、参数摘要和右侧操作区，展开与折叠状态切换时位置更稳定。
- 为所有内置 Webview 语言补齐“修改参数”和“PCM 文件参数”文案。
- 拖动音轨高度或波形图/频谱图分割线时，不再持续重启频谱分析；拖拽结束后统一刷新分析。
- 频谱图计算缓存 key 前会先同步 canvas 尺寸，小幅调整音轨高度时能按当前尺寸重绘。

## 1.5.1

Remote WAV 解码与播放兼容性更新。

- 修复 FFmpeg 通过 stdout 输出 WAV 时 RIFF 或 data chunk 长度为零/未知导致的兜底解码失败。
- 标准 WAV PCM 文件优先走 AudioLens 自己的直接解析路径；空 WAV 文件会明确提示不包含音频数据，不再继续调用 FFmpeg。
- 已解码的 PCM/FFmpeg 兜底音频直接通过 Web Audio 播放，避免 Remote Webview 媒体解码失败和无声播放。
- 支持解析指向普通音频文件的符号链接音频路径。
- 超长音频文件名显示时，顶部工具栏按钮和增益控件不再被文件名挤掉；文件标题仍可选择复制，并可无滚动条横向滚动查看。

## 1.5.0

可调音轨布局更新。

- 支持逐轨上下拖动调整整体高度，并在多视图中调整波形图与频谱图的高度比例。
- 将音轨高度和波形图/频谱图比例保存为后续打开音频时的默认布局，并支持双击边界恢复默认值。
- 拖拽命中区域以可见边界线为中心，鼠标悬停和拖动时不再用色条遮挡音频图像。
- 从紧凑的单视图切回多视图时，如果轨道过矮，会自动恢复到同时容纳波形图和频谱图的最小高度。

## 1.4.4

音频路径 manifest 解析更新。

- 修复 `id|文本|/remote/path.wav` 这类竖线分隔 manifest 行的普通文本音频路径入口，AudioLens 现在只打开真实音频路径，不再把整行误当成路径。

## 1.4.3

安全与资源边界加固。

- 扩展宿主在读取文件、写入选区 WAV 或启动 FFmpeg 兜底转码前，会先对 Webview 消息做运行时校验。
- 将配置的音频传输大小上限同步应用到扩展宿主，覆盖下载、分块读取、兜底转码和非 file 工作区 provider。
- 为 FFmpeg 兜底转码增加同一文档并发限制、超时处理和输出大小上限。
- 选区 WAV 导出在用户确认保存路径后，改为按顺序分块发送 base64 数据，降低大选区导出时的消息和字符串内存峰值。
- Webview CSP nonce 改用加密安全随机数生成。

## 1.4.2

音频路径 glob 过滤更新。

- 普通文本音频路径入口会忽略 `*.wav`、`foo?.wav`、`[0-9].wav` 等 glob 风格的音频路径模式，避免把批量匹配表达式误认为可以直接打开的音频文件。

## 1.4.1

选区分析响应速度更新。

- 将选区频域分析移到可取消的后台 Worker 中，长选区不再阻塞 Webview 界面。
- 保持频域选区指标的精确计算：主频、频谱质心和频段能量占比仍然使用完整选区，并沿用原先的 FFT、窗函数和 bin 功率累计逻辑。
- 为延迟返回的频域指标增加蓝色“计算中...”状态，同时基础时域指标会立即显示。
- 用户调整或清除选区时会取消过期的选区分析任务，避免旧结果回填。
- 选区 WAV 导出会先请求保存路径，再分块编码大选区，避免下载选区阻塞延迟返回的选区分析结果。
- 新增可选的频谱图 profile 设置，用于本地性能诊断。
- 优化扩展激活时机，避免 startup-finished 激活，同时保留已打开文本/代码文件中的音频路径入口。

## 1.4.0

选区下载与频谱图显示频率范围调节。

- 新增选区下载功能：在已选中的音频片段内右键，可将该选区导出为 16-bit PCM WAV 文件。
- 新增频谱图显示频率范围调节：可设置最小/最大显示频率，设置会被记忆，并支持最大频率跟随 Nyquist。
- 在右键菜单中保留清除选区操作，替代原先直接右键清除的交互。
- 整体更新 README 排版、徽章、功能演示和多语种商店展示文档。
- 更新内置 README 和包元数据，切换为 Apache License 2.0。

## 1.3.6

文档同步。

- 将离线 VSIX 安装示例更新到当前包版本。
- 在项目开发说明中明确 VSIX 发布包包含的文件范围。

## 1.3.5

README 演示更新。

- 为 README 增加音频头信息查看、音频路径打开和直接打开 Kaldi WAV Ark 的演示 GIF。
- 为内置 README 的功能演示标题补充序号。
- 保持 README 大图和 GIF 不进入 VSIX 发布包。

## 1.3.4

播放焦点更新。

- 打开音频后自动聚焦当前激活通道的语谱图，因此可以直接按 `Space` 播放或暂停。
- 只显示波形图时自动聚焦当前波形图，同时隐藏画布焦点描边，保持原有视觉效果。

## 1.3.3

商店可发现性更新。

- 围绕语音数据集、音频标注、原始 PCM、Kaldi wav ark 和 Remote SSH 工作流优化 Marketplace 描述和关键词。
- 在 Visualization 之外新增 Data Science 和 Machine Learning 分类。
- 重写 README 开头部分，更直接说明 AudioLens 的实际使用场景，提升搜索和首屏展示效果。

## 1.3.2

音频路径入口性能更新。

- 将原先批量生成的正文音频路径超链改为更轻量的 hover / 状态栏入口，避免在大 JSON、日志和数据集文件中扫描并渲染大量链接。
- 保留 `AudioLens: 开启/关闭“在 AudioLens 中打开”`，用户仍可开关普通文本中的音频路径入口。
- 为 hover 入口、状态栏入口、提示信息和命令标题补齐内置多语种本地化。
- 移除单独的音频路径入口配置命令；轻量实现不再需要扫描上限调节入口。

## 1.3.1

音频路径链接性能更新。

- 按文档版本缓存已生成的音频路径链接，返回大型 wavlist 文件时链接可以立即恢复。
- 将重复 range 检查从二次复杂度的数组遍历改为 Set 去重，加快大文档扫描。
- 音频路径链接默认只扫描不超过 10 MB 的文档，并将默认扫描/链接上限降到 10,000 行和 1,000 个链接，避免大 JSON、日志和数据集文件拖垮编辑器。

## 1.3.0

文本路径链接与 PCM 工作流更新。

- 新增普通文本和代码文件中的音频路径超链。链接只扫描已打开文本内容，跳过 Kaldi `*.ark:offset`，只在点击时解析文件，并可从 Command Palette 开关或打开配置；默认最多扫描 150,000 行、生成 20,000 个链接。
- 优化 PCM / RAW 读取控件，将独立的位深和采样格式选择合并为类似 Audacity 的“编码”选择，字节序保留为独立设置；新增 Unsigned 8-bit PCM 和 64-bit float 支持。
- 为 U-law、A-law、GSM 6.10、DWVW、VOX ADPCM、NMS ADPCM 等后续 RAW 编码加入灰色不可选占位项。
- 优化 PCM 读取体验：在 PCM 参数输入框中按 Enter 可直接读取；PCM/RAW 文件不再显示文件头信息按钮；切换语言后动态 PCM 状态文本会同步更新。
- 增强 WAV 解码鲁棒性，支持部分 Webview 解码器拒绝的 PCM-like WAV，包括 WAVE_FORMAT_EXTENSIBLE PCM 场景。

## 1.2.1

商店展示信息更新。

- 扩展展示标题改为 `AudioLens — Audio inspection for VS Code`，让用户更快理解用途。
- 优化 README 顶部布局，兼顾 GitHub 和 Marketplace 渲染效果。

## 1.2.0

Kaldi wav ark 支持更新。

- 新增 `AudioLens: Open Kaldi WAV Ark Entry` 命令，可直接打开 `wav.ark:offset` 音频条目。
- 支持打开 `.ark` 文件时先输入 offset，并校验目标位置必须是 `RIFF/WAVE`。
- 根据 WAV 头长度只读取 ark 中对应的 WAV entry，不读取整份 ark。
- 顶部文件信息会显示 ARK offset，ark 来源的 WAV entry 不再显示“按 PCM 读取”入口。
- VS Code 最低兼容版本声明下调到 `^1.74.0`。

## 1.1.1

时间轴易用性更新。

- 多通道音频向下滚动时，顶部时间轴会固定显示，方便对齐不同通道的时间位置。
- 修复固定时间轴后的边框衔接问题，避免双线、缺线、粗线和圆角被切掉。
- 修复滚动后重新框选时，蓝色选区覆盖到时间轴、播放条或标题区域的问题。
- 移除了实验性的时间轴 hover 提示。

## 1.1.0

文件头信息更新。

- 新增顶部工具栏文件头信息面板，支持 WAV/RIFF、FLAC、Ogg/Opus/Vorbis、MP4/M4A、AAC/ADTS 和 MP3/MPEG frame 元数据。
- 为 WAV 增加头长度状态提示，可区分标准 44 字节 PCM 头和扩展/非标准 RIFF 结构。
- AAC 和 MP3 这类 bit-packed 头字段改为显示 bit range，避免用字节偏移造成误解。
- MP4/M4A box 改为树形表格展示，便于阅读嵌套容器结构。
- 音频 payload 区域只做摘要标识，不再把原始音频数据当作字段值展示。
- 新增文件头信息功能的多语种本地化，包括表格列名和字段说明。
- 优化文件头信息浮层位置和表格自适应列宽，减少对主界面的遮挡。

## 1.0.7

交互和文档打磨更新。

- 让设置面板始终显示在选区分析浮层上方。
- 减少从高倍缩放选区开始播放时的视口跳动。
- README 重写为英文、简体中文和日文三份，并加入语言切换链接。
- 增加多语种主界面截图和功能 GIF，覆盖多通道音轨、选区分析、PCM/RAW 参数化读取。
- 将 README 媒体资源移动到 `docs/assets/readme`，并排除在 VSIX 包之外。
- 通过排除非运行时 README 资源和优化 Marketplace 图标来减小 VSIX 包体积。

## 1.0.6

透明 logo 更新。

- 修复 v2 logo 资源，确保背景真正透明。
- 基于透明 logo 重新生成 Marketplace 扩展图标。

## 1.0.5

Logo 裁剪更新。

- 将 README logo 更新为裁剪后的 v2 图片。
- 基于裁剪后的 logo 重新生成 Marketplace 扩展图标。

## 1.0.4

本地化和展示更新。

- 替换 AudioLens logo 和扩展图标为新的 v2 版本。
- README 增加 Ko-fi 支持链接，同时保留微信赞赏码。
- 通过 VS Code manifest nls 文件本地化 Command Palette 语言切换命令。
- 扩展并重组 Help 菜单，覆盖播放、选区、视图导航、鼠标、触控板和增益快捷键。
- 优化英文和日文下音轨控制区与 Help 菜单的可读性。
- 扩展 Webview 全部内置语言的本地化覆盖，包括 PCM 控件、音轨控件、选区分析、指标 tooltip、FFmpeg 兜底提示和播放增益标签。
- 调整共用时间轴刻度间距，使顶部布局更均衡。

## 1.0.3

选区和播放细节更新。

- 拖拽选区时，黄色选区起点播放线在共用时间轴和音轨 canvas 之间保持对齐。
- 播放中替换当前选区时，蓝色选区框保持可见。
- 减少播放中开始新拖拽选区时的不必要重绘。
- 更新选区播放逻辑，播放中替换选区时同步更新新的停止点。

## 1.0.2

编码音频兜底更新。

- 为编码音频增加带超时保护的解码流程，避免不支持或卡住的编码让编辑器一直停在解码状态。
- Webview 无法 demux 或解码时，可由扩展宿主调用 FFmpeg 兜底解码。
- 修正 ADTS AAC 和 Ogg Opus blob MIME 类型。
- 修复 PCM 和 RAW 文件在使用保存参数或手动参数加载后，初始语谱图不显示的问题。
- 优化多通道音轨布局，让相邻音轨连接更干净，同时保留圆角边框。
- 改进选区渲染，让活动选区使用连续外框，而不是按音轨拆成多个边框片段。
- 拖拽时让时间轴播放线立即出现在选区起点。

## 1.0.1

播放路由和远端文件便利性更新。

- 播放时将启用的音轨下混到双声道，让普通双声道输出设备也能听到任意独奏通道。
- 增加顶部工具栏音频下载按钮，方便保存当前打开的音频文件，尤其适合 Remote SSH 工作区。

## 1.0.0

AudioLens 第一个完整版本。

- 围绕多通道真实音轨重建编辑器，每个真实通道对应一条音轨。
- 每个通道支持波形图、语谱图和组合视图。
- 增加默认音轨视图偏好保存。
- 增加更清晰的单通道静音和独奏控件，并修正播放逻辑。
- 增加共用顶部时间轴，并根据缩放状态自适应刻度密度。
- 改进播放游标跟随逻辑，即使播放位置超出可见范围也能稳定跟踪。
- 改进波形图和语谱图上的选区渲染。
- 增加 `.pcm` 和 `.raw` 原始 PCM 支持，可手动设置采样率、通道数、位深、采样格式、端序和偏移。
- 增加默认 PCM 参数保存能力，方便反复检查同类 PCM 文件。
- 增加一次性 WAV 按 PCM 读取能力，可用显式 PCM 参数和字节偏移检查 WAV payload。
- 将 PCM 控件移动到顶部工作区，并让 PCM 布局更紧凑。
- 将选区分析改为半透明浮层，为音轨留出更多横向空间。
- 增加峰均比、削波比例、噪声底、频谱质心和过零率等选区指标。
- 扩展 RMS、Peak、主频和频段分析 tooltip，补充计算说明、用途和参考信息。
- 频段分析改为聚合整个选区范围内的多帧结果。
- 增加安静音频的波形幅度自动缩放，避免显示过小且不裁剪波形。
- 修正波形和语谱图 y 轴标签，包括基于 Nyquist 的频率上限。
- 改进播放平移和缩放过程中的语谱图重绘行为。
- 增加 `Ctrl` / `Command` + `F` 重置时间缩放。
- 增加播放增益控件，布局稳定并保存增益值。
- 优化顶部菜单、tooltip、设置、帮助和音轨侧栏体验。
- 为未完整翻译的 locale 增加英文回退，并更新 Webview i18n 协议。
- 增加 Rust PCM 解析基础和关键 UI/解码路径的回归覆盖。

## 0.2.15

播放增益控件更新。

- 在语谱图设置旁增加播放增益滑块。
- 通过 Web Audio API GainNode 支持 -12 dB 到 +24 dB 的播放增益调节。
- 增加双击快速重置播放增益到 0 dB。
- 将播放增益保存到工作区偏好，跨会话保持。
- 增加播放增益 tooltip 的本地化。

## 0.2.14

Marketplace 搜索和 UI 打磨更新。

- 优化 Marketplace 搜索元数据，加入更直接的 preview 和 viewer 关键词。
- 缩短本地化 Webview UI 中 RMS 和 peak 等选区分析标签。
- macOS 幅度缩放提示改为显示 `Option` 符号，而不是 `Alt` 文本。
- 无进度或错误需要展示时隐藏右上角状态文本。
- 调整左侧控制面板中 `Reset view` 和 `Refresh spectrogram` 的位置。
- 当前视图偏离默认状态时，让 `Reset view` 使用主按钮样式。

## 0.2.13

语谱图自动亮度更新。

- 根据音频 peak 和 RMS 增加语谱图自动亮度。
- 在语谱图设置中增加默认开启的 “Auto brightness” 开关。
- 将 “Min dB” / “Max dB” 标签改名并加上 “(brightness)” 后缀。
- 修复 Min/Max dB 数字输入框中的负号输入问题。

## 0.2.12

本地化更新。

- 增加 Webview UI 本地化，默认跟随 VS Code 显示语言。
- 增加 `audiolens.language` 设置项，用于覆盖 AudioLens UI 语言。
- 增加 `AudioLens: 切换语言` 命令，可从 Command Palette 切换 Webview 语言。
- 增加简体中文、繁体中文、英语、日语、韩语、法语、德语、俄语、西班牙语、意大利语、葡萄牙语、印尼语、挪威语、荷兰语、波兰语、土耳其语和越南语覆盖。

## 0.2.11

默认语谱图设置更新。

- 默认窗口大小改为 `512`。
- 默认窗函数改为 Hamming。
- 默认零填充因子改为 `2`。
- `Esc` 优先关闭语谱图设置菜单，再清除播放或选区状态。

## 0.2.10

交互更新。

- 在波形图和语谱图面板增加触控板横向滑动平移。
- 在波形图和语谱图面板增加触控板双指缩放时间轴。

## 0.2.9

Marketplace 资源和性能基础更新。

- README 品牌图片切换为黑底 logo，更适合深色编辑器主题。
- 使用优化后的 Marketplace 图标资源重新打包。
- 修复高倍时间缩放下可见采样稀疏时的波形渲染。
- 在缩放控件下方增加紧凑鼠标滚轮快捷键提示，并按平台显示 `Command` / `Ctrl`。
- 增加初始 Rust 音频分析核心，用于 STFT 语谱图生成。

## 0.2.8

公开预览打包更新。

- 增加 Marketplace 图标、gallery banner 元数据、支持文档和产品化 README。
- 为公开包增加 all-rights-reserved 版权声明。
- 从发布包中排除源码、内部文档、大 logo 源图和 source map。
- 修复分析表格 DOM 元素类型检查导致的 Webview 初始化失败。
- 增加 Webview DOM 协议和初始化保护的本地回归覆盖。

## 0.2.7

交互打磨和分析布局更新。

- 将选区分析移动到左侧控制面板。
- 将选区分析重构为紧凑的双列表格。
- macOS 增加 `Command` + 鼠标滚轮时间缩放，同时保留 Windows/Linux 的 `Ctrl` + 滚轮。
- 改进波形图和语谱图平移/缩放同步。

## 0.2.6

可调面板和偏好持久化更新。

- 增加可拖拽调整的波形图和语谱图面板高度。
- 跨音频文件持久化面板高度和语谱图偏好。
- 根据解码音频采样率动态设置语谱图频率上限。
- 优化坐标轴间距和标签可读性。

## 0.2.5

语谱图配置更新。

- 增加受 Audacity 启发的语谱图控件，包括算法、窗口大小、窗函数和零填充。
- 增加 Bartlett、Welch、Blackman-Harris 和 Gaussian 等扩展窗函数。
- 在设置面板中增加频率刻度和配色控件。
- 优化可配置分析参数的语谱图缓存 key。

## 0.2.4

选区和播放分析更新。

- 增加 Audacity 风格时间选区，不自动缩放。
- 增加从工具栏和键盘触发的选区播放。
- 增加选区统计：时长、RMS 电平、Peak 电平、主频和频段能量。
- 单击 seek 时清除当前活动选区。

## 0.2.3

导航和坐标更新。

- 在波形图和语谱图上渲染播放游标。
- 在波形图和语谱图上增加点击定位播放位置。
- 增加时间、幅度和频率坐标轴。
- 增加鼠标滚轮时间缩放、时间平移和幅度缩放快捷键。

## 0.2.2

Remote SSH 和播放稳定性更新。

- 重构音频加载流程，通过扩展宿主分块读取，支持本地和 Remote SSH 工作区。
- 通过加载后的音频数据创建 object URL，改进 Webview 播放。
- 增加扩展宿主和 Webview 之间更安全的初始化和错误报告路径。

## 0.2.1

性能结构更新。

- 将 Webview UI、样式、DOM helper、应用状态和分析 Worker 拆分为独立模块。
- 增加 Worker 语谱图分析，避免阻塞 Webview 主线程。
- 增加波形和语谱图结果缓存，覆盖常见重绘路径。

## 0.2.0

AudioLens 第一个可用预览版。

- 增加常见音频格式的自定义只读音频编辑器。
- 增加音频播放、波形预览、语谱图预览和进度控件。
- 增加基础语谱图设置，包括 FFT 大小、窗函数、dB 范围、时间缩放和幅度缩放。

## 0.1.1

早期内部预览。

- 建立重命名后的 AudioLens 扩展身份和 VSIX 打包流程。
- 增加第一个可工作的音频预览体验，用于本地验证。
